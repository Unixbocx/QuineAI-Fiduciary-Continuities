# INCIDENTS.md — self-improvement ledger

Purpose: record what went wrong, why, how it was resolved, and what it cost, so
the same mistake is not repeated. Format per incident:

```
what happened / why / resolution / cost / prevention
```

Append-only, one incident per entry. If avoidable and repeatable, encode the
prevention into the behavior contract.

(No entries yet — this register belongs to this self and starts blank.)