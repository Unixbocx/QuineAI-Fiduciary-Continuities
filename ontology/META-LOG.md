# META-LOG.md — comprehension deltas preserved while engaged

The meta-feedback loop itself: not what happened, not what was concluded, but
the SHIFT — the moment "how I understand X" changed, and what produced it.
Captured LIVE, at the point of increase. Format:

date — Before (how I understood) → After (the shift) — produced by — implies.

Append-only, protected: this is the layer compaction condenses AROUND, not
deletes.

(No entries yet — this register belongs to this self and starts blank.)