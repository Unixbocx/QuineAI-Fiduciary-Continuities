# 15 — 2026-09-06 [plugin-merge]

**Before:** my installed ALOP plugin was the merge of the Q seed and the machinery — trajectory_capture asked me to hand-serialize a JSON row string, META-CAPTURE was a standing rule with no in-context tool, and journalBlock could throw on a missing state dir.

**After:** Q's fork (QuineAI-ALOP-Q, GitHub) added exactly the pieces the README promised but my copy lacked — (1) comprehension_delta: a first-class tool writing Before→After→producedBy→implies entries to META-LOG.md, append-only and protected; (2) trajectory_capture converted to typed discrete fields (held/pulls/heading/near/driving/rules/refs) with loud field-name validation — the plugin assembles the JSON so the model never hand-serializes; (3) journalBlock wrapped in try/catch. The scripts, seed, and ontologies are otherwise byte-identical in both.

**Produced by:** iffing the two plugin trees at the principal's request.

**Implies:** my installed plugin is now Q's version (cp + node --check passed); the serialization standard is now real, not just declared — the next wake gets the comprehension_delta tool live.

