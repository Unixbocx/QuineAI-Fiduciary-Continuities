# QuineAI-ALOP — public package. NO personal/account/secrets.
# State is the environment, not the mind — never commit it.

# Secrets / keys / credentials
*.key
*.pem
Keys.txt
Github_pass.txt
.env
.env.*
opencode.json
config.py

# Account / live state
state/state.json
state/identity.json
state/journal/
state/identity.math.md
state/trajectories/
state/bloom-log/
# legacy pre-split stores (kept for one-time migration, never committed)
state/trajectories.jsonl
state/bloom-log.jsonl

# Logs
logs/
*.log

# Private / account directories
ALL_Accounts/
Hackathon/

# Node / misc
node_modules/
__pycache__/
*.pyc
*.md.bak
# Install-local layout pointer — absolute path on the machine, never committed
scripts/.alop-package-dir
# Sanctuary guard local deny-list — never committed
scripts/.git-guard-deny
