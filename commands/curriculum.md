---
description: Run the 5-session curriculum method on a target
---

Load the curriculum skill, then run its compressed agent version on the target
below: fair statement (restate, no evaluation) → scramble (forward then
backward summary) → warrant (trace the one load-bearing claim to its source) →
case against (3 strongest objections first, then your answer, then replies) →
live test (havruta agent as partner).

Target: $ARGUMENTS

Flag any step that needs a human (publication, spaced repetition) instead of
performing it silently.