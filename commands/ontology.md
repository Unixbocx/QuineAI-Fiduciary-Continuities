---
description: Query or update the ontology
---

Load the ontology-query skill. Consult ontology/ before answering. If the model
is silent on the question, say so — do not invent. Only write back to
ONTOLOGY.md / ANTI-ONTOLOGY.md / incidents.md when a real change is being made.

Question: $ARGUMENTS